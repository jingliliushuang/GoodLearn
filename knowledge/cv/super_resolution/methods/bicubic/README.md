# Bicubic 双三次插值

使用 OpenCV 的 `cv2.INTER_CUBIC` 进行下采样与上采样，模拟 x2 超分流程。

## 原理

双三次插值使用 4×4 邻域的 16 个像素，通过三次多项式插值估计新像素值，比双线性插值更平滑。

## 流程

1. 将原图下采样至 1/scale
2. 再上采样回原尺寸
3. 与原图对比计算 PSNR/MSE

## 参数

- `scale`：缩放倍数，默认 2
