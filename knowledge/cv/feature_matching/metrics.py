"""Feature matching evaluation helpers."""
import numpy as np


def evaluate(clean: np.ndarray, recovered: np.ndarray) -> dict:
    """Not used for feature matching runs; metrics come from process()."""
    return {}
